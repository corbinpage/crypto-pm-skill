{
  "name": "customer-feedback-triage",
  "version": "0.1.0",
  "private": true,
  "description": "Daily routine: crawl a Notion subtree via Claude+MCP, triage extracted feedback against the backlog, and stage review rows in a Notion triage table.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.91.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
